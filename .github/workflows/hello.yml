name: Hello World Workflow

# Trigger workflow on any push
on: [push]

jobs:
  hello_job:
    runs-on: ubuntu-latest   # runner environment
    steps:
      - name: Checkout code
        uses: actions/checkout@v3   # action to pull repo code

      - name: Print Hello
        run: echo "👋 Hello, GitHub Actions!"
